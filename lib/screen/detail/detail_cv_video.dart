import 'package:flutter/material.dart';

class DetailCvVideoScreen extends StatelessWidget {
  const DetailCvVideoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
